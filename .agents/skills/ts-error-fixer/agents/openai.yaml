interface:
  display_name: "TS Error Fixer"
  short_description: "修复 TypeScript/React 类型报错，保持最小改动并完成验证"
  default_prompt: "先读取报错，再定位相关文件，做最小范围修复，最后运行验证并按结构汇报。"

policy:
  allow_implicit_invocation: true
